# Optional — balance / sim scripts only:
# BALANCE_RUNS=500
# SEASON_RUNS=100

# Weekly board uses Netlify Blobs on deploy (no secrets required for anonymous submit).
# Local API: npm run dev:netlify
